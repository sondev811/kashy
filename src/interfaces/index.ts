import { Coords } from "google-map-react";

export interface INavbar {
    link: string;
    name: string;
    title: string
}

export interface ILocation {
  lat: number; 
  lng: number;
  name: string;
}